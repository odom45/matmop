org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.enableJetifier=true

# --- Release signing (local only) ---
# To produce a signed Play-ready bundle locally, generate a keystore and set these in your Gradle properties (do NOT commit secrets):
# RELEASE_STORE_FILE=C:\\path\\to\\your-upload.keystore
# RELEASE_STORE_PASSWORD=your_store_password
# RELEASE_KEY_ALIAS=upload
# RELEASE_KEY_PASSWORD=your_key_password
